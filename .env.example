# Use deterministic placeholders for a credential-free local workflow.
IMAGE_PROVIDER=mock

# To use real GPT Image generation, set IMAGE_PROVIDER=openai and add a key
# in .env.local. Never commit that file.
OPENAI_API_KEY=
OPENAI_IMAGE_MODEL=gpt-image-1.5

